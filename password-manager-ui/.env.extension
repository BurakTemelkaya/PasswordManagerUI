# Extension Ortamı Ayarları
# npm run build:extension sırasında bu dosya yüklenir

# API Base URL (Production server - Extension için)
VITE_API_BASE_URL=https://passwordapi.buraktemelkaya.com/api

# Uygulama Adı
VITE_APP_NAME=Password Manager

# Debug Mode (Extension'da kapalı)
VITE_DEBUG=false

# API Timeout (Extension için standart timeout)
VITE_API_TIMEOUT=30000
